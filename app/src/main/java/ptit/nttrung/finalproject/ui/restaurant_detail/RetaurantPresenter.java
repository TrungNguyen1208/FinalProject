package ptit.nttrung.finalproject.ui.restaurant_detail;

import ptit.nttrung.finalproject.base.Presenter;

public class RetaurantPresenter extends Presenter<RestaurantView> {
    @Override
    public void attachView(RestaurantView view) {
        super.attachView(view);
    }

    @Override
    public void detachView() {
        super.detachView();
    }
}
